import React, { Component } from "react";

class Logout extends Component {
  render() {
    return (
      <div className="container">
        <button className="profile--button--edit-profile">Logout</button>
      </div>
    );
  }
}

export default Logout;
