// import React from "react";
// import ReactDOM from "react-dom";
// import App from "./App";

// Passing done to your callback will force your tests to be asynchronous
// If that happens you must call done() at the end of the callback or else the test will timeout and fail
test("should pass", () => {
  expect(1).toBe(1);
});
